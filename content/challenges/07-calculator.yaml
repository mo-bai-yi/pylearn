title: 简易计算器
description: |
  编写四个函数：add(a, b)、sub(a, b)、mul(a, b)、div(a, b)，
  分别实现加、减、乘、除。

  然后依次调用四个函数计算 30 和 5 的运算结果并打印。

  预期输出：
  ```
  30 + 5 = 35
  30 - 5 = 25
  30 × 5 = 150
  30 ÷ 5 = 6.0
  ```
initial_code: |
  # 定义四个函数
  def add(a, b):
      # 你的代码
      pass

  def sub(a, b):
      # 你的代码
      pass

  def mul(a, b):
      # 你的代码
      pass

  def div(a, b):
      # 你的代码
      pass

  # 测试
  a, b = 30, 5
  print(f"{a} + {b} = {add(a, b)}")
  print(f"{a} - {b} = {sub(a, b)}")
  print(f"{a} × {b} = {mul(a, b)}")
  print(f"{a} ÷ {b} = {div(a, b)}")
tests:
  - input: ""
    expected: "30 + 5 = 35\n30 - 5 = 25\n30 × 5 = 150\n30 ÷ 5 = 6.0"
